<h1>Welcome to Like Medium</h1>
<ul>

<% @posts.each do |post|%>
  <li>

  <% if user_signed_in? %>
    <%= link_to "#{post.title}", post_path(post)%> | added by <%= link_to post.user.email, profile_path(post.user)%>
  <% else %>
    <%= post.title%>
  <% end %>
  
  </li>
<% end %>
</ul>